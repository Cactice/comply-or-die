const Index = () => (
  <div>
    <video autoPlay width={window.innerWidth()} height={window.innerHeight()}>
    <source src="/start.mp4"
            type="video/mp4"/>
            </video>

  </div>
)

export default Index
